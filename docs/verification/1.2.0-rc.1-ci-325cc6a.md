# Resource Multiplier 1.2.0-rc.1 clean-checkout verification

## Scope

This note records the exact GitHub Actions evidence for the structured-tooltip follow-up and distinguishes clean GitHub artifacts from earlier local Windows artifacts. It does not close the manual gameplay or named third-party compatibility gates.

## Production correction

Commit `d03d909259630382e757faf90c94e2d87116108a` introduced composed hover text for structured rows. When visible row fields are clipped, their complete values are included before supplemental tooltip details. The combined component still uses Minecraft's standard `Tooltip.splitTooltip` path.

The first clean workflow run for that commit exposed an existing static validator that required a specific one-line call shape. The implementation was not weakened. Follow-up commit `325cc6a6fcafe5810000f3c377e9e96ea5bd68c9` retained the composed tooltip behavior while restoring the validated call shape.

## Successful clean workflow

- Repository: `chedidandrew/Resource-Multiplier`
- Commit: `325cc6a6fcafe5810000f3c377e9e96ea5bd68c9`
- Workflow: `Build and verify`
- Run ID: `33351410406`
- Result: success
- Artifact ID: `9743770202`
- Artifact name: `ResourceMultiplier-325cc6a6fcafe5810000f3c377e9e96ea5bd68c9`
- Artifact ZIP digest: `320027C95703F814143805C73490522A20450AB1F0D2C8AD654100F5BDC096EC`

Every configured workflow step completed successfully:

1. Checkout on the exact commit.
2. Java 25 setup.
3. Gradle setup and wrapper validation.
4. Package, release-packaging, Mod Menu, tooltip-composition, edge-case, polish, and core validation.
5. Mapped JUnit, all 66 dedicated-server GameTests, and the Loom build.
6. Client GUI and authority GameTests under Xvfb.
7. Playable-JAR upload.

The validation step reported:

- 118 production Java sources
- 254 Git-tracked deterministic source entries
- 90 dependency-free core assertions
- structured tooltip composition checks passing

The dedicated runner reported all 66 required GameTests passing. The main build completed successfully, followed by a successful client GameTest run.

## Clean artifact inspection

The artifact contained one playable file:

```text
resource-multiplier-1.2.0-rc.1.jar
```

Verified JAR identity:

- Size: `601,231` bytes
- Entries: `311`
- Java class major: `69`
- SHA-256: `B974CABC64698679A81B7E39915CD3092815BDF91406C6E5B9E2DBEBF0A403C9`
- Public name: `Resource Multiplier`
- Version: `1.2.0-rc.1`
- Mod ID: `smart_resource_drops`
- License: MIT
- Production icon: present
- Homepage, source, and issue metadata: present
- Nested JARs: none
- GameTest/testmod content: none
- Runtime worlds, caches, logs, configuration, or user data: none

## Hash distinction

Three separately produced candidate artifacts are intentionally recorded with distinct identities:

| Source state and environment | Size | SHA-256 |
| --- | ---: | --- |
| Local Windows presentation build | 601,044 bytes | `5CA797D6BC4BBAB6F223361D9A99A118850F40B0273426D3B1ADF8AAB5CDCB31` |
| Clean GitHub presentation commit `3925ef1c` | 601,094 bytes | `F565C5A8CCFE0FD0519900D1C46CB7FF1EFA2097CF2FD6800D821D5AE9BA98B5` |
| Clean GitHub tooltip-fixed commit `325cc6a` | 601,231 bytes | `B974CABC64698679A81B7E39915CD3092815BDF91406C6E5B9E2DBEBF0A403C9` |

The project does not claim cross-platform byte-for-byte reproducibility from these results. The guarded release workflow will generate the authoritative checksum for the final tagged `1.2.0` artifact.

## Warnings observed

The clean workflow completed successfully while displaying headless-runner or upstream warnings, including missing Linux audio and narrator libraries, offline authentication, software OpenGL, JOML Unsafe use, and Node or GitHub Action runtime deprecation notices. These warnings did not originate from Resource Multiplier gameplay code and are not counted as manual gameplay evidence.

Action-version refreshes should be handled as a dedicated pinned-dependency maintenance change rather than mixed into this verified candidate correction without review.

## Release status

This clean result closes the exact-candidate automated workflow gate for the tooltip-fixed production code. It does not close:

- hands-on block, entity, XP, explosion, provenance, shearing, and budget behavior;
- GUI visual and input review at multiple resolutions;
- separately installed multiplayer authority testing;
- existing-world restart and migration testing;
- live datapack reload testing;
- dense-output observation;
- named and versioned third-party compatibility cases.

`release_ready` must remain `false` until those gates are complete.
